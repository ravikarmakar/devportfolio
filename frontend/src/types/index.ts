export interface User {
  _id: string;
  name: string;
  email: string;
  bio: string;
  profileImageUrl: string;
  socialLinks: Object;
  currLocation: string;
  mobileNumber: string;
  createdAt: string;
  updatedAt: string;
}
